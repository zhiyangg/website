{
    "name": "website",
    "type": "module",
    "version": "1.0",
    "scripts": {
        "build": "astro build",
        "preview": "astro preview",
        "deploy": "astro build && gh-pages -d dist",
        "postinstall": "gh-pages --version"
    },
    "dependencies": {
        "@astrojs/sitemap": "^3.2.1",
        "@astrojs/tailwind": "^6.0.0",
        "astro": "^5.4.1",
        "astro-icon": "^1.1.5"
    },
    "devDependencies": {
        "gh-pages": "^6.3.0"
    }
}
